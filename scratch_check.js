const fs = require('fs'); const dotenv = require('dotenv'); const envConfig = dotenv.parse(fs.readFileSync('.env.local')); async function fetchSupabase(table, query) { const res = await fetch(envConfig.NEXT_PUBLIC_SUPABASE_URL + '/rest/v1/' + table + '?' + query, { headers: { 'apikey': envConfig.SUPABASE_SERVICE_ROLE_KEY, 'Authorization': 'Bearer ' + envConfig.SUPABASE_SERVICE_ROLE_KEY } }); return await res.json(); } async function check() { const res = await fetchSupabase('operating_hours', 'select=*'); console.log(JSON.stringify(res, null, 2)); } check().catch(console.error);
